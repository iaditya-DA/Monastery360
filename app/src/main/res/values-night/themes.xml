<resources>

    <!-- Night Theme -->
    <style name="Theme.Monastery360" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- ✅ Flickering Prevention for Night Mode -->
        <item name="android:windowBackground">@color/black</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

</resources>